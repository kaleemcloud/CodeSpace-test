name: 05-1-filters-activity-types
on:
  pull_request:
    types:
      - closed
    branches:
      - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "running when PR is closed"

